{
  "name": "app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "mkdir -p dist/ && cp -R ./questions ./dist/ && parcel index.html",
    "build": "mkdir -p dist/ && cp -R ./questions ./dist/ && parcel build index.html",
    "deploy": "sh deploy.sh"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "hi-base64": "^0.2.1",
    "js-base64": "^3.6.0",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-router-component": "^0.40.0",
    "tailwindcss": "^2.0.1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "sass": "^1.29.0"
  }
}
